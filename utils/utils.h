#ifndef _UTILS_H_
#define _UTILS_H_

#include <stddef.h>

extern size_t strqex(const char*, char*, size_t);
extern size_t strbex(const char*, char*, size_t);
extern size_t striex(const char*, int*, size_t);

#endif

